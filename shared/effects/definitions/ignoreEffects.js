export const ignoreEffectsEffect = {
  description: "Permite que un personaje reciba estados, pero ignore los efectos indicados cuando se resuelven. ignoreEffects es una lista de tipos de efecto.",
  fields: ["type", "duration", "targets", "ignoreEffects"]
};
